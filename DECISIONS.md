# Decision Log

A running record of points where my judgment redirected the AI's default. The split in [WORKFLOW.md](WORKFLOW.md) says I direct and the AI implements — this is the evidence for that claim, kept deliberately so it shows the full range: roadmap-level strategy down to one-line craft fixes. Nothing here is the AI's idea that I rubber-stamped; each is a call I made against what the AI was about to do.

Entries are factual — what the AI defaulted to, what I decided instead, and why. The reasoning is meant to carry the weight, not adjectives.

## Strategic and architectural calls

| Date | The AI's default | My call | Why |
|---|---|---|---|
| 2026-09-25 | Make the Anthropic adapter Day 1 of v0.7, so my Claude API credits are spent this sprint driving the runs that test the cost-serving feature. | **It stays in v1.0 beside the multi-backend benchmark.** I had assumed v0.7 was making iterate Claude-compatible so my credits could test cost serving; what settled it is that the serving cost is the infra or API needed to serve the winner, not the driver's chat cost, so the wall is testable today on any priced OpenAI-compatible target, and Claude adds a driver, not a test. | A day the cost-serving work and the twelve carry-ins are already fighting over. If I want the credits spent sooner it is a v0.7.1 patch, certified on one prompt run and one table run. |
| 2026-09-25 | Name the deterministic serving-cost check "the budgeter". | **"The Pricer", and its refusal is "the price gate"**, one word like Linker and Critic, beside the lever gate it sits next to. And when the price gate refuses a candidate it names the first entry on the ready line, top down, that fits the budget, never the cheapest: no reward for cheaper, the same rule the wall itself has. | A budgeter sounds like it sets the budget. The pricer is arithmetic over a dated table and never reads an abstract, which is what lets it refuse before a fit runs and keeps it from inventing a price. |
| 2026-09-21 | Put the layer stack in the brief and let the coding agent turn it into a `fit()` call, the way every other lever works. | **Hand the coder the finished call.** For the two layer classes the task message carries `fit(layers=[...])` or `fit(head=[...])` already written, and the coder is told to run exactly that. The EASY PATH sentence names `layers=` and `head=` too, so "from zero" does not read as an invitation to write a network by hand. | Every other lever is one word and a number, and a brief that says "6 epochs" cannot be mis-copied. A stack is six pieces, and the whole feature is that the network the user or the paper named is the network that trains. Measured on gemma4:12b at the run's own temperature, 10 calls a case: with the new coder prompt the first cell was the right `fit()` call 10 of 10 with the call line and 10 of 10 without it, so the line did not rescue a failure here. It is insurance on the case the probe cannot reach, a stack longer than the example with widths the model has no habit for, and it costs one sentence on two briefs a run. |
| 2026-09-21 | Let the Researcher suggest an architecture and trust the citation, since the paper index is already resolved by the harness and cannot be faked. | **A suggested stack survives only if the abstract we showed the model states widths at all and every number in the stack appears in it. The stack is what gets dropped, not the finding: a suggestion that also names a real model keeps its line with the widths taken out, and one that was nothing but a stack goes whole.** | The index guarantee says the paper is real. It says nothing about whether the paper contains the network. A 12B copies the example it is shown, so `conv(32) pool conv(64) linear(256)` would come back with a real DOI against it, and the lever ladder opens a layer class on a finding, so an invented network would become a real experiment with a citation on it. That is the one bug this specialist exists to prevent, and it does not get an exception for architecture. Measured on a planted paper: an abstract that writes a stack out keeps it 5 of 5, and one making the same claim with no width anywhere had a stack invented for it 5 of 5 and dropped 5 of 5. The width rule is there because bare number matching passed an invented stack 5 of 5 on an abstract whose 64 px, batches of 32, dropout 0.2 and 10 classes happened to be the numbers the model made up. Dropping the whole suggestion was my first call and it was wrong: it threw away the model name and the citation, which are the parts the paper really earned, for the sake of a head nobody can use. |
| 2026-09-21 | Hand the coder the layer call and let every other setting fall where it falls, the way a brief for any other lever does. | **The from-zero call carries its own epoch count.** `fit(layers=[...], epochs=20)`, never `fit(layers=[...])` on its own. A head call still names no epochs. | The coder copies the handed call character for character and then fills the rest in from the recipe in front of it, which is the pretrained best at 3 epochs. 17 of 18 recorded first cells re-stated `epochs=3`, and `merge()` reads a stated epoch count as the choice, so the from-zero network trained 3 epochs instead of the 20 it gets when nothing is passed. The stack then loses on a schedule rather than on its architecture, two such losses close the class, and a typed ask buys exactly one experiment. A head is the opposite case: it sits on the carried best and keeps the best's epochs on purpose. |
| 2026-09-21 | Treat a user's typed steer as words for the supervisor only, since the ladder is built on measured numbers and a sentence is not one. | **A typed ask is a recorded fact, and it opens `layer-stack`, `custom-head` or `last_block` for exactly one experiment.** A "no" word anywhere in it opens nothing, prose opens nothing, and the user gets a line back saying what was read. | A steer that could not reach the lever gate was the gap: the supervisor would brief the stack, the gate would reject it as not ready, and the user would watch their own instruction bounce. The ask is a fact about this run in the same way a fit line is, so it can open an entry, and every guard still runs on whatever the model writes from it. The limits are what keep it honest: "never build from scratch" is a ban and must not open the thing it bans, "try a custom CNN" names no network, and an ask that stayed open forever would pin the run on one stack instead of buying one experiment. |
| 2026-09-21 | Let `cut=1` take a whole pretrained stage off a backbone under that name, and accept a layer list that ends in a linear layer as wide as the class count. | **Name it `drop_stages` and print what it removed, and refuse a last linear that equals the number of classes.** "Remove the top and put these layers" is what `head=` already does; the stage version is a different, much bigger move and has to say so. | On resnet50 `cut=1` throws away about 15M of 23.5M pretrained weights, and nothing in the old name says that. A 12B copying a paper writes the class count as its last layer and the harness then adds a second final layer after it, which makes a bottleneck as narrow as the class count and no error anywhere. Both are legal values, so neither can be caught by a range check; they need a name that means what it does and a refusal that names the fix. |
| 2026-08-01 | Pick the run's metric with a dedicated, narrow `choose_setup()` pass — look at the data profile, name a metric — kept separate from the Researcher's technique work. | **The metric must come out of the FULL research, not a small pass run just to get a metric.** One retrieval, two judgements over the same papers. | Two problems with the narrow version. It would have run research twice, once pre-loop for the metric and again at iteration 1 for techniques. And the metric — the thing the entire run optimizes and the one number that carries across runs — would have been decided by the THINNER of the two passes, grounded in a class balance instead of in literature. Sharing the retrieval means the reasoning compounds, and it costs FEWER calls than before, because iteration 1 no longer researches separately. |
| 2026-08-01 | Keep the metric set a closed, hand-written registry (12 entries), and defer widening it to v0.5 so the Critic and the metric dial could land first. | **The metric set cannot be a fixed list, because we do not know what the Researcher may propose** — and widen it TODAY, not next sprint. | Once a specialist can propose techniques it can propose metrics, so a hand-picked list is the wrong shape for the thing being built next. The AI's objection was that a model supplying DIRECTION could silently invert the loop, which was right, but it argued from that to keeping the set small. The resolution was to derive both from scikit-learn's own scorer registry: 54 metrics, direction taken from the sign convention of the library that defines each metric. Open vocabulary, and still impossible for an agent to invert the loop, because it never supplies the direction. |
| 2026-08-01 | Cut v0.4 down to fit the one build day that was left: ship the Critic plus probability metrics, move the Researcher and the metric-picking dial to v0.5, and state the cut in the release notes. | **Ship everything v0.4 promised.** No scope cut; do the whole day's plan and release on the Sunday as committed. | The release table in the README is a public commitment, not a private plan. Two of the five v0.4 promises (literature-aware proposals, agent picks the metric) depend on the Researcher, and one of them is a row in the moat comparison table. Quietly shipping half and re-labelling the docs to match is exactly the move that makes a build-in-public trail worthless. The cadence is the product: if a Sunday can slip its contents, nothing else in the ladder means anything either. |
| 2026-05-27 | Build breadth-first — every problem type first, turn the agent on around Week 7. | Re-sequence to **agent-first**: the agentic loop is the v0.1 milestone; everything after is capability expansion. | Building breadth before the agent buries the differentiator. Sequence toward the thing that matters first. |
| 2026-05-28 | Leave sandboxed code-gen ("the agent writes + runs any training code") late in the roadmap. | Bump it **early to v0.2**, right after the v0.1 loop. | "Run the model the research actually recommends" is the real unlock — it shouldn't wait behind six other versions. |
| 2026-05-27 | Frame cost-constrained optimization as *the* moat. | Reframe as the **specialized combination**; cost-aware serving is the *flagship*, not the only moat. | One feature is not defensible. The defensibility is the combination plus the domain specialization. |
| 2026-05-28 | Candidate spec as a flat `{"model": x, ...hyperparams}`. | Nested `{"model": x, "params": {...}}`. | Clean separation of *which model* from *its params*, no key collisions, and the exact shape the agent will emit from research. |
| 2026-05-26 | Treat a reported/prior baseline score as the number to beat. | **Never trust a reported score** — always re-measure the baseline through our own eval. | A number computed a different way is not comparable; you cannot honestly claim an improvement against it. |
| 2026-05-29 | Take `--baseline` + `--source` as context only, no behavior; defer reproduction to Week 10. | **Make the inputs do something in v0.1** — by end of Week 3, source must reconstruct the baseline approach through our eval and then improve from there. | A baseline number we never use is dead weight. If you take an input, it has to drive something. |
| 2026-05-29 | Implied user-provided source code would be executed in the v0.2 sandbox to reproduce a baseline. | **Never execute user-provided source code — ever.** Source is read-only text the LLM uses to reconstruct the approach; the v0.2 sandbox runs the agent's OWN generated code, never the user's. | User notebooks/scripts are untrusted (malware/RCE-adjacent); a sandbox reduces but doesn't eliminate the risk. Reconstruct-from-text is the right permanent policy. |
| 2026-05-30 | Defer all interactive-CLI features (pause, mid-run chat, streaming, Ctrl-C) to the Week-12 Streamlit UI. | **Split into two milestones.** v0.2 picks up the *cheap* interactive wins (live progress display, streaming LLM responses, graceful Ctrl-C) alongside sandboxed code-gen; v0.3 is a new focused milestone for *full mid-run chat* (pause/resume/conversational state). Streamlit UI shifts to v0.10. | Waiting until Week 12 for any interactivity is too late; lumping it all into v0.2 balloons that milestone. Two focused milestones ship each piece when it's actually ready. |
| 2026-06-01 | Keep it single-agent (one Proposer LLM in a deterministic loop) — multi-agent adds coordination cost for little gain. | **Go multi-agent after v0.2**, at the Researcher milestone (v0.4): specialist agents (Researcher, Proposer, Critic) report structured, typed output to a **supervisor agent** that decides. v0.1/v0.2 stay single-agent; built on our own harness, not LangGraph. | Specialists with one focused job tool-call far more reliably (mitigates the local-model yield problem), and a supervisor that reasons over digested high-quality context makes better decisions than one LLM juggling everything. The Researcher is the natural single-to-multi transition. |
| 2026-06-02 | On the code-gen path, bound the agent to a library list (either an allow-list or one auto-derived from the installed environment, fed into the prompt). | **No library boundary at all** — the agent imports whatever it wants and we install its imports before running. The environment adapts to the code, not the code to the environment. Resolution is deterministic (AST extraction, not a second LLM); auto-install happens only in the disposable sandbox, never the user's local venv. | An allow-list is the wrong frame for "the agent writes its own code" — it re-imposes the v0.1 cap we're lifting. A bad/exotic import just fails its install and becomes a captured failure + retry, so openness costs at most one iteration. The import→package **resolution architecture is provisional/TBD** (hand-kept alias map for now); to be revisited. |
| 2026-06-03 | Ship sandboxed code-gen behind a flag (`--code`), e2b as the safe default compute, and don't install into the user's local venv. | **Code-gen is the DEFAULT** mode and **local is the DEFAULT** compute; local install-on-demand is allowed **with consent**. A setup wizard saves these once so users don't retype flags. | Code-gen is the v0.2 headline — hiding it behind a flag buries it. Local-default is lower friction for a developer tool (running generated code is normal, like running any script) and is made a conscious choice by the first-run wizard. Consent (asked once) is what makes local install safe, which is what makes "no library boundary" actually true on the default path. |
| 2026-06-03 | Reconstruct a `--source` baseline as an allow-listed `{model, params}` spec, approximating anything off-list to the "closest equivalent." | **Reconstruct as CODE** — the Reconstructor writes a `train_and_predict` reproducing the source faithfully (real CatBoost, a custom net), no approximation. | Now that the agent runs its own code, forcing the user's existing approach through an allow-list throws away fidelity for no reason. The baseline should be *their* approach, measured through our eval — not our nearest guess at it. |
| 2026-06-03 | Feed only the final score (or error reason) back into the next proposal. | **Feed the run's stdout + error traceback back** to the code proposer (bounded, recent iterations). | If the agent can see what its code printed (class balance, dtypes, missing values) and the real traceback when it crashed, it learns the data and self-corrects — turning each run into exploration, not just a blind score. Leakage-safe because holdout labels never enter the script. |
| 2026-06-04 | History shows the proposer only a one-line description + score of past attempts. | Also feed a **deterministic component fingerprint** (`[used: SimpleImputer, OneHotEncoder, …]`, AST-extracted, no LLM) per attempt, and make the prompt **feature-engineering-first**. | Live runs showed the agent repeated the same impute+one-hot and only swapped the model, because it literally couldn't see prior preprocessing. The fingerprint makes "what was tried" visible; an A/B proved the FE staleness was prompt-bound (even a 70B only swapped models) — and the FE prompt then made local qwen engineer a new feature and beat every prior run. The richer LLM-summary version is deferred to v0.4. |
| 2026-06-04 | Build sandboxed code-gen as one-shot (write a whole `train_and_predict`, run once); defer interactive cell-by-cell to a later milestone (v0.3). | **Pull cell-by-cell execution into v0.2**: a stateful code-interpreter session where the LLM writes a cell, sees its real output, then writes the next — within one experiment. | One-shot is blind: the agent writes the whole pipeline without ever looking at the data, so it reflexively does generic preprocessing and, when pushed to engineer features, silently breaks the pipeline (near-zero scores it can't see coming). Looking-at-the-data-as-you-build is the capability unlock, and it's worth doing now rather than shipping a blind v0.2. |
| 2026-06-05 | Keep the multi-agent split for v0.4 (the Researcher milestone); v0.2 stays single-agent. | **Re-sequence: the Supervisor + Coding agent land IN v0.2** (coder-first build). The supervisor compresses runs 1..n-1 into a 1-2 line brief for run n's coder; specialists (Researcher/Critic) still graduate later at the same tool boundary. | Cell-by-cell already needs an across-experiments strategist to brief each session and decide stop — that IS the supervisor. Building it now (one LLM via tools) is the honest start of the multi-agent system, not a deferral; the tool boundary is where the future specialist agents plug in unchanged. |
| 2026-06-05 | Cap the cell-by-cell session by a max number of cells. | **No cell cap — the wall-clock deadline is the bound**, and the deadline charges KERNEL-execution seconds only, never LLM thinking time. (`max_cells` stays as a runaway backstop, not a budget.) | A cell count is the wrong unit: small inspection cells are cheap and good, and charging the local model's slow thinking against the budget would starve a weak driver of turns a fast one gets for free. Bounding by kernel-time gives any backend the same working budget. |
| 2026-06-06 | Let the agent treat `X_train`/`X_holdout` as ordinary mutable working data. | **Protect the canonical inputs in the harness** — snapshot them in the preamble and restore before every agent cell, so in-place mutation in one cell cannot silently poison later attempts in the session. | A weak model reflexively mutates `X_train` in place (`X_train[cols] = enc.fit_transform(...)`); across cells that corrupts the data every later attempt builds on. Immutability is a guarantee the model shouldn't have to think about — enforce it structurally, like verified-finish. |
| 2026-06-07 | Keep iterating the coder prompt in-session to force stepwise (cell-by-cell) code and push the f1 quality bar. | **Stop the in-session prompt loop; author the coder prompt out-of-band** to reach the quality bar, and reserve this session for the day-by-day build. Staged-vs-monolithic writing style is **model-bound, not harness-bound**. | Proven by runs: a 14B defaults to a complete script and reverts to monolithic cells whenever handed a working blob to edit; prompt wording can't reliably beat that, and forcing it regressed reliability (0.5813, 2/5) vs the monolithic harness (0.6353, 5/5). The harness lifts the floor model on score + reliability; it doesn't need to make a weak model *write* like a strong one. Stepwise R&D presentation, if wanted, belongs at the deliverable layer. |
| 2026-06-08 | Treat qwen3:14b as the fixed floor model and keep working the prompt/harness around it. | **Change the floor model — gemma4:12b deserved a chance** (and test thinking mode empirically too, rather than assuming it's not worth the latency). | The floor model is a choice, not a constant. Validated immediately: the unchanged harness ran 10/10 experiments in the 0.60–0.62 band on gemma — the first all-iterations-above-baseline run. The thinking half came back refuted (it made cells MORE monolithic and broke the supervisor's tool calls — now coder-only, off by default), but refuted-by-run beats assumed-by-opinion, and the experiment left two durable instruments: the coder-only think split and the thinking-trace-in-notebook debugger. |
| 2026-06-08 | Presented the staging-vs-score result as a tradeoff to weigh (staged R&D notebooks at 0.59 vs the unreadable monolith's 0.6353), with reverting to the monolith on the table. | **R&D staging is LOCKED; the score delta is acceptable.** Proper research-style notebooks — explore, decide, build in steps, conclude — are the product bar, not just the metric. | "If the code being written is in proper R&D style that's better." The notebook IS the deliverable a human reads; a number without a readable journey isn't the product. Also overturned the day-old "staging is model-bound" conclusion: it was model-bound for prose RULES; a worked example the model imitates fixed it (monolithic cells 31–35% → <1%). The score gap closed two days later anyway (0.6325 staged). |
| 2026-06-08 | Keep the Summarizer agent at v0.4 as planned; the supervisor's one-line history compression is enough for v0.2. | **Pull the Summarizer into v0.2 now**: one digest per finished notebook (what was tried, data insights, what helped/hurt, takeaway), stored in Memory, read by the supervisor — never whole notebooks as context. | The runs hit the exact wall the Summarizer exists for: winning techniques (TargetEncoder, threshold tuning) were discovered in one notebook and DROPPED by the next, because only a 1-line brief crossed between them. And whole-notebook context would hallucinate by iteration 5 — compression is the only scalable carrier. The digest is the unit of cross-notebook knowledge transfer. |
| 2026-06-09 | Land the prompt rewrites by committing and merging to main once tests pass. | **Open a PR and do NOT merge until a run satisfies us; every prompt rewrite isolated in its own commit.** `main` stays the known-good earlier-prompt baseline. | Prompts are the piece we're least sure of and most likely to want back: a one-commit `git revert` must drop a prompt alone and keep the infrastructure. Paid for itself the same week — the validate-never-submit regression and the supervisor rewrite both iterated on the branch while `main` stayed comparable. |
| 2026-06-10 | Treat the it2 missing-column loop as another error-recovery bug to patch around. | **State the design principle: open workspace, guarded boundary.** Restrictions exist ONLY at the validation boundary (sealed holdout labels, verified predictions contract, pristine canonical input frames); everything inside the workspace — any engineered feature, library, model — is open and persists in the model's own variables. Fix the guard's VISIBILITY, never loosen it. | "We need to give the model the ability to do feature engineering — that's what R&D is. Allow everything, but restrict some things so we can validate every run properly." The incident wasn't an FE restriction — the model's in-place patch was silently undone by the input-reset guard it couldn't see. The fix arms the model with the reset fact at the exact moment it loops (the same-error nudge), keeping both the open solution space and the validation guarantee. |
| 2026-07-04 | Proceed to the v0.2 release wrap-up (the capability list was done; the score had earlier been ruled acceptable), and treat the remaining quality work as post-release. | **The release is GATED on a quality bar, and the bar is trajectory, not a score constant**: staged R&D per notebook, pickup of the carried best, progression across notebooks, failure-knowledge transfer, and no silent process waste. Iterate on the current architecture until it holds; no new roadmap features ride along. | The notebooks ARE the product a reader opens; a release whose runs waste 60% of their iterations markets a number, not a working method. Score follows process: a run that measures nine levers fairly against a strong baseline and loses honestly is a good run. |
| 2026-07-07 | Keep chasing duplicate/null iterations toward zero with more guards. | **Certify on the capability floor**: after the guard stack, the residue (1-2 honestly-labeled duplicate or measured-null iterations per run) is the 12B being a 12B: detected, labeled in memory and in the notebook, and converted where a deterministic nudge can convert it. Chasing zero is chasing the model, not the harness. | 21 instrumented runs of forensics-fix-rerun: every process mechanism found got a deterministic guard + regression test, and the failure classes shrank from run-killing to one-bounded-slot. What remained was fairly-measured ties honestly reverted, which is correct R&D behavior, not waste. |
| 2026-06-13 | Ship all three "cheap interactive wins" the 2026-05-30 split promised for v0.2 — including token-level streaming of LLM responses. | **Ship graceful Ctrl-C and per-cell progress logging in v0.2; defer token-level LLM streaming to v0.3.** v0.2 closes with the run-loop interrupt handler (finalize Memory + keep earned notebooks, no stack trace) and a per-cell progress line (which cell, ok/error, kernel-time budget spent). | The cell-by-cell loop already solved liveness structurally — the unit the user watches is the committed cell and its real output, not the tokens en route to it, and a per-cell line makes a long local-model session visibly move. Streaming partial tokens through a tool-calling loop (parsing incomplete tool-call JSON, interleaving thinking traces, doing it across both the OpenAI and native-Ollama adapters) is real client work for a marginal payoff when cells are the unit. It belongs in v0.3 next to the mid-run chat that genuinely needs token-by-token output, not bolted onto v0.2 to honor a checklist. |

| 2026-08-08 | Build the eval suite as a script: sweep the corpus, print a table, done. Ship the headroom method as a product feature so users can read their own runs. | **Two calls. (1) It is INTERNAL — "that's for product development", so no `iterate eval`, nothing in the wheel. (2) It is a separate SUBSYSTEM with its own database, versions, dates and datasets, not a script.** | The internal half keeps the package honest: a user has one version installed and does not have these datasets, so shipping it means bundling data and supporting a feature zero users need. The subsystem half is the one that makes the tool survive: a full sweep is hours and released versions are frozen, so v0.2's number on churn can never change. A script recomputes the grid every time, which makes measuring too expensive to actually do — and a measurement tool nobody runs is not a measurement tool. Accumulating in a store means adding v0.6 costs one row. Proved itself on the first run: the harness immediately found that the flagship v0.4 result captured 55% of a properly measured ceiling rather than the 110% the hand method reported. |

| 2026-08-09 | Ship v0.5 today with prompt iteration for classification, and open v0.6 with regression as a second task type. | **Ship classification AND regression together; the release can slip a day or two.** And the scope is prompts doing ML/DL work — classification and regression — not transcription, translation or summarising. | "The common ones are mostly enums and scoring — if a photo task is there, companies want the colours he is wearing from an enum and a rating, because only then can they do something, because it's structured." Correct about the market and it reframed the gap I was aiming at: I had been treating free-text generation as the missing capability, when the actually-shipped LLM tasks are the two that produce structured output you can act on. "Prompt iteration for ML tasks" is also a claim worth two days, where half of it is not. The metric question follows from the same call: rating tasks want rank agreement (Kendall, Spearman, Pearson) which sklearn does not offer as scorers at all — so the registry grows by hand-curation, exactly as `rmse` and `brier` already do. |

| 2026-08-09 | Treat the missing capability as free-form generation — summarising, rewriting, translation — and either build a similarity metric or refuse it. | **Wrong gap. The scope is prompts doing ML/DL work: classification and regression.** "The common ones are mostly enums and scoring — if a photo task is there, companies want the colours he is wearing from an enum and a rating, because only then can they do something, because it's structured." Also: research the metrics, Kendall and Pearson are recommended for these. | A better read of what LLMs are actually deployed to do than mine. Both of his named tasks produce output a system can branch on; a summary does not. It also turned out to be the cheap direction — 16 regression metrics were already in the registry and the blocker was one hardcoded word. The metric half of the call was right too: sklearn offers none of Pearson, Spearman or Kendall as scorers, and they are the ones that see a rating prompt ranking correctly while mis-calibrated (pearson 1.000 where rmse says 3.000). |

| 2026-08-10 | Close the four cheap carry-ins, defer the free inspect step a third time (it needs a fourth `AttemptOutcome` and patience carve-outs), and ship v0.5. | **All seven. "1 to 6 actually do it all then only we will do the v0.5 release since it was promised that way."** | Holding the release to what was promised is what forced the useful discovery. Re-examined under the requirement to actually build it, the inspect step's three stated blockers all turned out to be consequences of one wrong assumption — that an inspection is an EXPERIMENT. The schema had been saying otherwise the whole time: `ExperimentResult` rejects a result with neither metrics nor an error. Modelled as a Researcher pass instead, it needs no new outcome, no patience carve-out and no memory record, and the change is smaller than the deferral note that kept postponing it. Two real bugs came out of the same push: no probability metric was scoreable on the sandbox code path, and the dossier's fact extractor was silently discarding "missing values: 11" because it contains the letters "val". Neither would have been found by shipping the cheap four. |
| 2026-09-08 | Certify and demo v0.5 on a dataset already in hand (toxicity, where three runs had reproduced the gain), and read a flat result as a hard-problem result. | **"It should not be like the ones that reach ceiling on baseline only."** The certification run goes on a dataset with measured headroom, so a flat result is a miss and a gain is real. Irony (SemEval-2018) was chosen after the literature said LLMs underperform on it and a sweep measured 0.02 of headroom over the minimal prompt. | A demo on a saturated dataset proves nothing in either direction. The run then found the runaway-record defect (#59), which the toxicity runs had never triggered because their few-shot layout was different. |
| 2026-09-08 | Explain a slow prompt run by local Ollama latency and suggest an API backend would be faster. | **"Entirely API calls taking time is no reason to argue time."** The cost is variants times records times latency. Latency is the user's hardware; the multiplier is the harness's, and the agent was scoring about eight full 100-record variants per iteration. Small-slice-then-promote scoring is the item that cuts it roughly fourfold, planned for v0.6 and moved to v0.7 on 2026-09-16. | Blaming latency hides the term the product controls. Measured the same day: a 60-record cell needed 600s not because calls were slow but because one record never stopped generating, which is a harness bug, not a hardware fact. |
| 2026-09-13 | One input file per run, split here 80/20 and stratified, for every family; a vision dataset only as a CSV of image paths. | **The user may hand over the split, for every family, and a folder is a valid image input.** `--data` alone means we split; `--train` plus `--holdout` means the split is theirs and the holdout is sealed exactly as given, for tabular, prompt and vision runs alike. A class-folder tree is accepted as it ships, with or without `train/` and `test/` inside. | "If he feels not secure giving us the entire dataset, he can give us a train and a test." A team with a canonical test set, or one that does not want a tool reshuffling its data, keeps control of the one thing every score depends on. It also closed a hole the CSV-only design had hidden: a user's own folder names the class in every path, so the adapter now copies every image under a name derived from its own bytes and shuffles row order once with a fixed seed before a kernel sees anything, which makes that leak impossible instead of something the Critic has to catch. |
| 2026-09-14 | Copy every image the kernel needs under an opaque name and stop there: the linked dataset exists only as CSVs and a cache the agent reads. | **The linked data is laid out as a folder a person can browse and reuse.** `raw_files/` with a copy of everything given, then `train/` and `holdout/` we build, class subfolders for classification and images directly inside for regression, plus the CSVs. "They give a data flag and a folder path, then we make a data folder, a raw_files folder with all the files, and in the same data folder our own train and holdout." | The user gets their dataset back in one standard form whatever shape it came in, which is the deliverable of the linking step and the thing they can hand to anyone. Their originals are never touched. The kernel keeps seeing byte-named copies, so the browsable class folders cost nothing on the leak surface. |
| 2026-09-14 | Ship the linker as its own command, `iterate link`, that writes the folder and prints the run command to use afterwards. | **One command.** "I wanted iterate run and it should figure out, get confirmation and start working." A folder given to `iterate run` is linked, shown, confirmed and run in one go; `--target` is not asked for when the label is found for you. | Two commands is a workflow the user has to remember and a place for the two halves to disagree. The linking is a step of the run, so it lives in the run. Until the vision target and the run switch land the run stops right after the folder is written and says so, and the same guard now stops a plain CSV of image paths, which would otherwise have gone into the tabular loop on file names. |
| 2026-09-14 | Build the data linker as one PR. | **Three PRs, in build order, each usable on its own:** the rules ladder with the canonical folder; the Linker with the pause; the monitor. "Keep the PRs clean and in scope." | Each changes a different surface, a loader, then a prompt-driven specialist, then a report, so a review holds one idea at a time, and the first is worth merging even if the other two change. The Linker is named as the sixth specialist and reads the folder only where rules cannot settle it; whatever it answers is rebuilt and re-measured before anyone sees it, and the pause is a conversation: yes, no, or a correction in plain English. |
| 2026-09-15 | The monitor only reports: a proven leak across the split, the same image bytes on both sides, is a warn a person reads, and nothing is ever dropped. | **Warn, never stop, and the copies come out of the holdout.** "If we have a leak the user can say remove these from holdout and we can remove, right? When we do our train and holdout from the given data we just remove duplicates from test data." When the split is ours, no holdout given, no train and test pair, no split column, a holdout image whose bytes sit in training is left out of the holdout and the report says how many. When the split is theirs the warn lists the pairs and the pause takes `drop` or `remove` to take them out of the holdout; the dropped rows are listed in `monitor.json`, counted in the linked block, and the drop is remembered with the plan. | A stop would send the person off to edit data by hand, which the monitor exists to spare them, and under their own split it would refuse the holdout they sealed. A twin scored from memory lifts a holdout score by at most its share of the holdout, so the honest fix is to take it out of the holdout, which is a copy anyway; `raw_files/` keeps every original. |
| 2026-09-15 | A class with fewer than 5 training images is a warn, and a 20 to 1 imbalance is a note. | **No hard-coded count.** "Don't hard-code a number like 5; minority less than 20 times is fine." The class check is the ratio only: a note when the largest class holds more than 20 times the smallest, and a warn for a holdout class with no training images at all. | The metric is chosen from the data profile and the literature, so an imbalance is already answered where it matters, by measuring with a metric that does not reward the majority class; the note is information, not a gate. A count like 5 is a guess about someone else's data; the structural floor, at least as many images on each side of the split as there are classes, is the only absolute the split itself needs, and that one is a refusal with the way out; a class the split still leaves on one side is a warn. |
| 2026-09-16 | The image baseline is the frozen resnet18 probe: ImageNet features and a logistic regression on top, the strongest cheap floor. | **The baseline is a plain CNN trained from zero with basic prep, a classifier or a regressor.** "How do we know what model to select? The baseline should be a simple CNN, a very simple fixed architecture, like we do for tabular: a predefined, very basic cleaning so the model can do a complete run without any heavy lifting." Three blocks of convolution, batch norm, ReLU and pooling at 32, 64 and 128 channels, a pooled linear head, 64 px, 20 epochs, cosine schedule, no augmentation. Pretrained probes and fine-tunes become tries the agent makes, with research choosing the model. | Which pretrained model to start from is a research question, so putting one in the floor answers it before the run asks it. The table path already works this way: a fixed default model with basic cleaning, then research, then iterations; "99% similar to tabular, just the data is different and the models are different." The floor also got honest about how much there is to find. Measured on MPS: EuroSAT 0.950 against a 0.986 ceiling, Flowers102 0.554 against 0.974, storm wind 13.1 knots against 26.6 for guessing the mean. The cosine schedule is part of the recipe because a constant rate swung EuroSAT between 0.823 and 0.937 over the last three epochs. |
| 2026-09-16 | The vision target scores classes only, and a number label on an image is refused until a later release. | **Every mode predicts both kinds of task.** "Every new mode we add will have everything it can predict. I am building for ML and DL models to predict both types of tasks, classification and regression." Image regression ships in v0.6: the same target, a one-output head trained on labels scaled with the training mean and spread, a ridge probe, the regression panel, and a number-label example, storm wind speed from NASA satellite frames, split by storm. | A mode that predicts half the tasks is half a mode, and the gap would sit in LIMITATIONS for a release while every user with a number label hit it. The work is small against the target that exists: the head width and the loss change, and nothing else about a run does. Building it found a scoring bug on the table path too: a whole-number regression target cut fractional predictions before scoring. |
| 2026-09-16 | Keep the baseline under the same 540-second cap as every try, trimmed to the epochs that fit and flagged when it is cut. | **No time cap on the baseline.** It always runs its fixed 20 epochs. | A baseline trimmed by the machine's speed is a different model on a laptop CPU than on a GPU, and the number every try is measured against would move with the hardware. It runs once per run, so waiting for it is cheap; on an Apple M5's CPU with no GPU, at 0.072 s a step on 4 threads, EuroSAT's 20 epochs take about 9 minutes. |
| 2026-09-16 | Build the image run as two PRs: the image target learning numbers and the new baseline, then the run itself. | **Three PRs.** First the baseline and number labels, then what a run may read and what a cell may touch, then the run itself. | The file rules and the cell sandbox grew out of review into their own surface, and they apply to every local run, not only images, so they deserve a review of their own rather than riding inside the vision PR. |
| 2026-09-16 | Number the sprint's build days one per PR, so the three linker PRs counted as Days 2, 3 and 4, the image target became Day 5 and the run Day 8, and the release moved a week for them. | **A day is a calendar day, and one day can carry several PRs.** "I don't think we really took 7 days to complete the linker PRs, each one day, so we can make correct days, multiple PRs, if that's the case." The sprint table was rewritten by real dates: #63 on Sun Sep 13, #64 and #65 on Mon Sep 14, #66 on Tue Sep 15, #67 and #68 on Wed Sep 16. | The table is the record of how fast the work went. Counting a PR as a day hid that: by the end of Wed Sep 16 the old table showed Friday's row already done, and a public release had moved a week for linker work that took two days. |
| 2026-09-16 | Keep the release on Sun Sep 27, with the carry-ins on Mon Sep 21 and Tue Sep 22, as the sprint section had it after the linker was added. | **Release this coming Sunday, Sep 20.** "We need to get all 7 days done right before the release." The image work fills Thu Sep 17 to Sat Sep 19, Sun Sep 20 holds the RTX 4050 validation, the certification run and the release, and the twelve carry-ins move to v0.7. | The linker did not cost the days the re-date assumed, so the week it moved comes back. The carry-ins are prompt-path and eval-suite work that none of v0.6's image promises depends on, and holding the release for them would delay the part users are waiting for. |
| 2026-09-16 | Guard the workspace with a word check on the cell's code, for image runs only: refuse a cell that names `.iterate`, `raw_files`, `link.json`, `monitor.json` or `holdout/`, keep the Mac sandbox off for image runs, and leave tabular and prompt runs as they shipped, since changing them inside an image PR looked out of scope. The harness itself still read any image path a CSV gave it. | **"Do not touch the files" holds for every local run, and a run uses only the data it was given.** "Do not touch the files should apply to all types of local runs, because we are making the data gathering and storing in our place, our format. The other thing is, when the user gives data or train and holdout folders, we should use only those and not touch anything outside." | The memory, the run folders and the linked data sit in `.iterate` for every kind of run, so a guard for image runs leaves the same files open to a tabular or prompt cell, and a check on words is not a lock: a cell can reach a file without typing its name. The harness is a reader too: on main a holdout CSV in another folder read its images from the training CSV's folder, and 6 of 6 holdout rows were training images. Now the harness refuses an image path or a link that leads outside what was named before any file opens: a refusal through a link names the link and, for a folder or a CSV of relative paths, prints a quoted copy command to paste, a row that leaves by `..` or an absolute path is told to move the images or drop the rows, and a link loop or a link that leads nowhere is told to remove the link. On a Mac every local kernel runs inside the macOS sandbox and opens only its own folder, the files the run gave it, its Python and one folder for model weights, and no kernel gets the harness's API keys, since a tabular cell printed planted OpenAI and Anthropic keys on main. A link I made is refused too, because the harness cannot tell it from one a download made, and the copy is cheap on a Mac: a 264 MB clone took 0.006 s. Linux and WSL2 have no sandbox yet, so those runs say their cells are not confined. |
| 2026-09-16 | Refuse a cell that runs an installer in image runs only, by a word check on its code, and keep the rule out of the tabular and prompt runs that already shipped. | **A cell never runs an installer, in any run.** When I put "do not touch the files" on every local run I asked "for do not install, is that still applicable?" It is, for every run: the harness installs, a cell never does. At the Day 5 gate I took the pick that puts `--compute e2b` under the same rule. | On a local run a cell that runs pip itself skips the install consent, the rule that torch and torchvision never change mid-run, and the constraints file, so every install comes through the one door the harness plans and logs. That door stays open for the research (next row). On e2b the harness installs into its disposable sandbox anyway, with no constraints file, so a refused cell loses nothing there, and the agent learns one rule, not two. The check reads the whole cell before it runs: on 836 honest cells from past runs it flagged none, at 0.53 to 0.55 ms a cell, and it caught 44 of 46 adversarial installer cells and 26 of 36 more shapes the review wrote; a cell that only asks what is installed, like `!pip list`, is pointed at `importlib.metadata`. It is a name check, not a boundary: the 2 misses build "pip" from pieces, and obfuscated calls such as `vars(os)['system']`, an aliased `exec` or ctypes, and a script one cell writes and the next runs, pass it too. On a Mac a sandboxed cell cannot write to any environment, so a package it gets in can only land in its own folder. The sandbox also refuses installer programs by name in every PATH folder, which the interpreter's own pip gets past: `python -m pip install --target` into the cell's folder worked on a sandboxed kernel. On Linux nothing stops it yet. |
| 2026-09-16 | Lock installs down for the image run: the notebook kernel installs nothing at all, and every install pins the exact version of everything already installed, so a package that needs a different version of anything present fails. | **Installs must not block a package the research needs.** "Doesn't this restrict our ideal goal of research-based work, if the research finds this is the ideal model for this problem and it requires a package we don't have?" The harness installs a missing import through four routes a dry run picks: install as is, restart the kernel with it, save it for the next run, or refuse. On a restart losing what the kernel held: "yes, the losing of cached images works, to make our runs succeed." | Research that names a model the run cannot load is research thrown away, and a lock that refuses every clash becomes the limit on the research. uv's dry runs work out what an install would change and change nothing, so the route is picked before the environment is touched. Measured in a throwaway venv on a sandboxed kernel: tabulate planned the install route in 0.52 s and installed in 0.16 s; plotly 7.1.0 over a loaded narwhals 1.14.0 planned a restart, and the kernel restarted in 0.975 s and re-ran the cell; sktime 1.1.0 would move pandas under a running iterate, so it was saved, and the next `iterate run`, started from another folder, installed it before pandas loaded. A package that does not exist, pycaret against iterate's own pandas, and torch were refused, each with its reason. A restart costs the variables and the cached images, which the run rebuilds; a refusal costs the idea. On `--compute local` torch and torchvision still never change mid-run and every install carries a constraints file; e2b installs into its disposable sandbox as before. |
| 2026-09-16 | The image run's first cell tells the agent not to import torch and not to write a training loop, so every try trains one of the three backbones `fit()` knows. | **The agent may train any model the research names, so the sandbox keeps weight downloads working.** "If the research finds this is the ideal model for this problem, given all the stats at that point", the run has to be able to use it. | A fixed model list in the harness answers the research question before the research runs, the same reason the baseline is a plain CNN. `fit()` stays the easy way to train resnet18, resnet50 and convnext_tiny, and from Fri Sep 18 the agent may write its own torch code for anything else, with the harness still holding the data, the sealed holdout, the time limit and `submit`. That only works if a sandboxed cell can download weights, so while the sandbox is on TORCH_HOME and HF_HOME point at `~/.cache/iterate/weights`, a folder the cell may write, the network stays open for every family, and HF_TOKEN stays in the kernel for gated models while every other key is removed. Closing the network for tabular cells was measured: every download failed. The cost is one more download of a model already cached elsewhere, 5.3 to 8.7 s for resnet18 with a forward pass. Without a sandbox the caches stay where the user keeps them, since moving them buys no isolation and only forces downloads. |
| 2026-09-16 | Skip the metric step for images: the harness picks the deterministic default by target type, `--metric` is the only other way in, and the agent choosing its own metric stays a table and prompt step. | **Images keep the metric step, in image wording, and an explicit `--metric` still wins.** The agent chooses from the image profile and the papers as it does on a table, limited to the metrics that can actually score these labels. | Choosing the ruler is the v0.4 dial, and taking it back for one family would leave the family with the least familiar data getting the least reasoning about how it is measured. An image label is not self-explaining either: the same tiles can want accuracy, `f1_macro` or a probability metric depending on how balanced the classes are and what the answer is for, which is a question about the literature and the profile, not about the file type. The cost is visible and taken on purpose, and LIMITATIONS says so: both example datasets usually pick `f1_macro` while their READMEs quote accuracy (Flowers102 3 of 3, EuroSAT 2 of 3 over three measured runs), so `--metric accuracy` is what reproduces a stored ceiling and the evals always pass it. The step runs even when the paper search comes back empty, because a failed search must not quietly decide how a run is measured; `--no-research` is the way to the deterministic default, and the run prints which it used and why. |
| 2026-09-20 | Ship v0.6.0 on Sun Sep 20 as drafted in #75. | **Hold the release and add the saved model and the layer params.** "The user has to run the notebook to get the model he is going to plug in his production app", and the notebook did not even run. | A release that trains a model and throws it away is not a release. An image run saved no network anywhere, and Run All failed at cell 1 for every family since v0.2. The research can name an architecture, so fit() has to be able to build one from a list of layers, and attach a head to a pretrained backbone, which is the most common thing people do. New date Tue Sep 29. |
| 2026-09-20 | Fill the docs with RTX 4050 numbers: CUDA against MPS epoch time, the VRAM peak and the out-of-memory capture, in the README, the three example READMEs and the launch thread. | **"Lets not put gpu times, just put that GPU can also run."** The docs say GPU compatible, Apple and NVIDIA, and quote no timings. | The laptop check answered the question that mattered: torch saw CUDA, the baseline trained on the GPU, and an out-of-memory fit was caught cleanly. A timing table invites a benchmark argument the project is not about, and a reader only needs to know their GPU will work. The exact evidence, and what has not run on CUDA yet, stays in LIMITATIONS and BUILD_LOG. |

## Craft and detail calls

| Date | The AI's default | My call | Why |
|---|---|---|---|
| 2026-08-01 | Write the BUILD_LOG session entry as work progressed and mark the sprint row **partial** while the PR was still open. | **Log a session only when it is finished, and mark it done.** Code and docs go in the SAME PR, which merges once the day's work is complete — so an entry always describes a completed session, and an open PR never means partial. | An entry that tracks work-in-progress turns the daily trail into a status board, and "partial" then just reads as "the PR has not merged yet," which says nothing about what was built. Finishing the work is the fix, not relabelling it. It also keeps the log honest in the other direction: the row goes to done because the five items exist, not because the day ran out. |
| 2026-05-27 | Treat slow model tests as "probably my laptop," or fix on a hunch. | Fix **only if measurement proves** it's a real bug, not the hardware. | Don't optimize on vibes — and a 10s fit on 120 rows is never the hardware. Measuring found a ~200x thread-oversubscription bug. |
| 2026-05-25 | Use emoji status markers in the public docs. | Drop decorative emoji; words for status, plain `✓`/`✗` in matrices. | Emoji-as-status reads "AI-generated / beginner" in serious infrastructure repos. |
| 2026-05-27 | Leave the BUILD_LOG inconsistent (Week 1 had a "Done?" column; Weeks 2–3 didn't). | Add the column everywhere. | Asymmetry in the public trail looks careless. |
| 2026-05-26 | Date a research entry to the day it was written up. | Correct it to the day the research actually happened. | The decision trail is only useful if it's accurate. |
| 2026-05-27 | Mixed day-numbering in the session notes (plan-position vs calendar). | Reconcile to calendar-day headers with plan-position in parentheses. | One unambiguous timeline. |
| 2026-05-30 | When `/v1` can't disable qwen3 thinking, bundle the workaround into `OpenAICompatibleClient`. | Build a separate **`OllamaClient`** adapter on Ollama's native `/api/chat` with `think:false`; the OpenAI client stays clean for cloud backends. | One backend's quirk shouldn't pollute the shared client. The `LLMClient` protocol is the seam — adding a sibling adapter is the right shape. |
| 2026-05-30 | Hardcode prompt strings inside `core/proposer.py`. | Centralize every prompt in `src/iterate/prompts/prompts.yaml` (a loader exposes `PROMPTS`); modules reference structured keys. | Prompts change far more often than logic; one file = one place to review and iterate wording. |
| 2026-05-30 | Accept `--baseline X` alone (informational-only number; we'd still use the factory default for actual measurement). | **`--baseline` requires `--source`.** A number with no source describes nothing we can run, so the input is dead weight on its own. | Either it drives behavior or it shouldn't be a flag. Explicit CLI error is better than silent ignoring. |
| 2026-05-30 | `--fresh` should delete the existing memory db (true reset). | Archive it instead — rename to `<name>.YYYYMMDD-HHMMSS.bak`, then create a new clean db. Triggered by `--fresh`, `--source`, or `--baseline + --source` — any explicit "new chapter" signal. | Recoverable cheap safety net; honors the "start over" intent without losing forever. The user can `rm` the `.bak` if they really want it gone. |
| 2026-05-31 | Output the best model as just a config + score in the summary. | **Save the winning model as a usable `joblib` artifact** (+ a `best.json` config sidecar), printed with a load line. | "We found the best model" is hollow if the user has to rebuild it themselves. The contract promised an *artifact* — deliver one they can `joblib.load(...).predict(X)`. |
| 2026-05-31 | Ship every dependency (incl. forward-looking ones) in the core install. | **Lean core deps** — drop the unused `sqlalchemy`; move `e2b` (sandbox/v0.2) and `kaggle`/`datasets` (future adapters) to extras. | `pip install iterate-ai` should pull only what v0.1 actually runs on; speculative deps belong behind extras. |
| 2026-06-03 | Keep e2b behind a `[sandbox]` extra (`pip install 'iterate-ai[sandbox]'`) per the lean-core rule. | **Bundle e2b into core** so the only command is `pip install iterate-ai`; `--compute e2b` then needs just an API key. (Supersedes the e2b half of the lean-core call above; kaggle/datasets/etc. stay extras.) | Users don't optimize install commands — they expect one boring line and accept it taking time. Bracket-extra syntax is friction for no payoff when the dep (e2b) is now a v0.2 default-path backend and is a light HTTP SDK. Lean-core still holds for genuinely optional/heavy deps. |
| 2026-06-04 | A cloud `--backend` alias (groq/openai/…) needs the user to also pass `--base-url`, or it falls back to the local Ollama URL. | **The alias supplies its own base URL** (`groq`→`api.groq.com/openai/v1`, + openai/together/deepseek); explicit `--base-url` still overrides. | Surfaced by a saved-config run that 404'd: storing `backend=groq` without a URL was useless. The alias already names the provider, so it should imply the endpoint — which is what makes "the setup wizard need only provider+model+key" actually work. |
| 2026-09-08 | Put a ten-line comment above two new constants explaining the measurement that justified them. | **"Why adding so many comments? See how other params are not having so many comments."** Constants get no block. A comment states a constraint the code cannot show; a measurement, an anecdote or an argument goes to BUILD_LOG. Applied across the 59 places the same pattern had accumulated (#60), with the AST of every file proven identical before and after. | A comment carrying a measurement rots silently the day the number changes; the log is dated and never edited. 885 comment lines became 748 and nothing measured was lost, because the six measurements that lived only in code moved to the log. |

---

Each entry plays out in the public trail: [BUILD_LOG.md](BUILD_LOG.md) for what shipped and [RESEARCH_LOG.md](RESEARCH_LOG.md) for the sourced reasoning behind the bigger calls.
