"""Internal evaluation harness. Not part of the shipped package.

Answers one question the unit suite cannot: is `iterate` getting better from one
version to the next? Same datasets, same floor model, same budget, one version
changed. See README.md in this directory for why it is not a product feature.
"""
